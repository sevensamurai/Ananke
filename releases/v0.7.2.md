# Ananke v0.7.2

**Theme: documentation site now live — the `docs/` directory is automatically deployed to GitHub Pages on every push to `main`.**

No library or API changes. This patch wires up the GitHub Actions workflow (`.github/workflows/pages.yml`) that publishes the full Ananke documentation site to GitHub Pages. The site covers 16+ guides (Getting Started through Agentic Patterns), a structured FAQ, Reference pages, and the `nnke` tool companion guide — all auto-deployed from the `docs/` folder whenever it changes.

## Breaking changes

None.

## Package updates

| Package | Change |
|---|---|
| All `Ananke.*` | Version bump to 0.7.2 (no functional changes) |
| `nnke` | Version bump to 0.7.2 (no functional changes) |
