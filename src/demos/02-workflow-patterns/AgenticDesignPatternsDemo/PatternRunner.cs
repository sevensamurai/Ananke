namespace AgenticDesignPatternsDemo;

/// <summary>Shared console utilities for pattern runners.</summary>
internal static class PatternRunner
{
    internal static void PrintHeader(string title) =>
        Console.WriteLine($"--- {title} ---");
}
